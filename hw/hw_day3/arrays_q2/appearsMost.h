#define OK 0
#define SIZE_ERROR -1
#define MAX_SIZE 40

/**
   Description- checks which num in the array appears most frequently.
   Input-  arr: the declared array from the main
          _size: the size of arr[]
   Output-  the most frequently number in the array
   Error-   X
 **/
int appearsMost(int arr[], int _size);
