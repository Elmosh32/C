#define OK 0
#define SIZE_ERROR -1
#define MAX_SIZE 40

/**
   Description- prints all the even numbers
   Input-  arr: the declared array from the main
          _size: the size of the arr[]
   Output- @return the number of even numbers
   Error-  @return SIZE_ERROR -1 if the array size is bigger from 100
 **/
int evenNums(int arr[], int size);

/**
   Description- prints all the odd numbers
   Input-  arr: the declared array from the main
          _size: the size of the arr[]
   Output- @return the number of odd numbers
   Error-  @return SIZE_ERROR -1 if the array size is bigger from 100
 **/
int oddNums(int arr[], int size);
